<%- include("partials/header"); -%>

<h1>Home</h1>
<p>
    <%= home_content %>
</p>

<%posts.forEach(elements=>{%>
    <h3><%=elements.Title%></h3>
    <p>
        <%= elements.content.substring(0,100) + "..."%>
        <a href= "/post/<%= elements.Title %>">Read more</a>
    </p>
<%})%>

<%- include("partials/footer"); -%>
